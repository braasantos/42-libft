/*#include "libft.h"

char *ft_itoa(int n)
{
  char *ptr;
  ptr = (char)malloc((n + sign) * sizeof(int))
  if (!ptr)
    return (NULL);
  else 

}

int main()
{
int load = 15;
char buffer[100];

sprintf(buffer,"%d",load);

printf("buffer in int = %s\n",buffer);
    return 0;
}*/